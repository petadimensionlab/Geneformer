# ISP 記述ルール（用語・表現・根拠）

> # ⛔ 最初に必ずこのページを読んでください
>
> **ISP に関する作業、記述、レビューを始める前に、必ずこのページを読んでください。**
> 書き方、用語、表記、判定の語彙で迷ったときは、**このページが唯一の基準です**
> （各ページの書き方より、このページのルールを優先します）。
>
> - 判断に迷ったら、**推測で埋めずに「判定不能」と書いてください。**
> - 数値の基準は [評価基準](criteria.md)、用語の意味は [用語と手法の定義](glossary.md)、
>   実行時の行動規範は [遵守項目](checklist.md) にあります。
> - 競合や重複の裁定の記録は [§9](#9-競合重複の記録) にあります。

## 0. 30 秒で引く索引（何を決めたいとき、どこを見るか）

| 決めたいこと | 見る節 |
|---|---|
| どの語を使うか | [§1 用語](#1-用語これだけを使う) |
| どう書くか（文体、主語、参照） | [§2 表現](#2-表現) |
| 数値をどう書くか（桁、単位、出所） | [§3 数値の表記](#3-数値の表記) |
| 根拠と判定をどう書くか（合格、目安、未検証） | [§4 根拠と判定](#4-根拠と判定) |
| どの文書に書くか | [§5 文書の分担](#5-文書の分担) |
| ページやレポートの骨格 | [§6 骨格](#6-骨格)、[§7 レポート](#7-レポートdocxpdf) |
| 用語や基準を変えたい | [§8 変更手順](#8-用語や基準を変えるときの手順) |
| 以前と食い違う記述を見つけた | [§9 競合・重複の記録](#9-競合重複の記録) |
| どこまで適用済みか | [§10 適用状況](#10-適用状況) |

## 0-1. 適用範囲

このルールは `docs/isp/` の全ページ、テンプレート集（`templates/`）、レポート（DOCX と PDF）、
およびそれらを生成するスクリプトの日本語と英語の文面に適用します。

---

## 1. 用語（これだけを使う）

同じものを別の言葉で書くと読み手が迷います。**左の列を使い、右の列は使いません。**
初出では真ん中の列のように意味を一度だけ添えます。

| 使う表記 | 意味（初出で添える説明） | 使わない表記 |
|---|---|---|
| **ISP** | in silico perturbation の略。計算機上で遺伝子を操作する解析 | in-silico（ハイフン付き） |
| **Shift（Shift_to_goal_end）** | 遺伝子を削除したとき、細胞の状態が goal（例: 健常）へどれだけ近づいたかを表す値 | シフト、シフト量 |
| **削除** | 遺伝子を 1 つ取り除く操作。この Wiki の基本操作 | KO（データ列名を除く）。実験操作の説明として「ノックアウト」は可 |
| **摂動できる細胞数** | その遺伝子のトークンを持ち、実際に削除の対象になった細胞の数 | 摂動対象細胞数、削除できる細胞数 |
| **検出率** | 対象の細胞のうち、その遺伝子を持っている細胞の割合 | 発現率 |
| **遺伝子リスト** | 解析対象として選んだ遺伝子の集合 | 遺伝子パネル、遺伝子セット |
| **細胞プール** | 対象にする細胞型の集合。初出以降は「プール」と省略可 | 細胞集団、セルプール |
| **状態対** | 比較する 2 つの状態の組（`start_state` と `goal_state`） | state pair、状態ペア |
| **状態分離** | 埋め込み空間での 2 状態の隔たり | separation、分離度 |
| **相対距離** | 距離 ÷ 2 つのベクトルの平均ノルム | 分離率、relative distance |
| **ノイズ床（2.1e-04）** | 計算誤差だけで符号が反転しうる Shift の大きさ | 閾値、しきい値 |
| **微調整（fine-tuning）** | 分類器を目的のタスクに追加学習すること | fine-tune、ファインチューン（日本語の文では使わない） |
| **符号が反転する**（名詞は「符号の反転」） | 同じ遺伝子で、2 つの条件（モデル、組織、時点）の Shift の正負が食い違うこと | 符号が逆、符号反転 |
| **細胞型** | 単球や T 細胞といった区別。データの列名は `celltype` のまま | 細胞集団、細胞種 |
| **健常（WT）** | 比較の基準になる非疾患側 | 正常、コントロール（本文では使わない） |
| **疾患（AD、PD）** | アルツハイマー病（AD）、パーキンソン病（PD）。初出では日本語名を添える | 病態（文脈が不明確なとき） |
| **モデル（V2-104M、V2-316M）** | Geneformer の事前学習済みモデル。数字はパラメータ数を表す | 104M、316M（初出前の単独使用） |
| **数値精度（fp32、bf16）** | 計算に使う浮動小数点の形式 | dtype だけでの言及 |
| **分類器（CellClassifier）** | 細胞型を判定するために追加学習したモデル | ヘッド、probe |
| **移動度** | 埋め込みベクトルの動きの大きさ。Shift の別名として使わない | — |
| **基準（E1〜E6）** | [評価基準](criteria.md) で定義した 6 つの観点。旧称は C1〜C6 | C1〜C6（古い文書を読むときだけ参照する） |
| **目安** | 実測例の間を取った線。**合否には使わない** | 基準（目安と基準を混ぜない） |
| **根拠の種類（A、B、C）** | A は自分たちの実測、B は他組織や他データからの借用、C は未確立 | 根拠の明示なし |

---

## 2. 表現

1. **一文で一つのことを書く。** 接続詞で 2 つ以上の主張をつながない。
2. **主語と述語を省略しない。** 「〜を記録」ではなく「〜を記録します」と書く。
3. **記号で語句を並べない。** 「A／B／C」ではなく、読点でつなぐか箇条書きを分ける。
4. **略語は初出で意味を添える。** 例: ISP（in silico perturbation、計算機上での遺伝子操作）。
5. **記号だけの要約を書かない。** 情報を詰め込むときは表にする。
6. **同じものは同じ語で呼ぶ。** 本文と表で別名を使わない。
7. **断定と推定を分ける。** 推定には「推定」と書き、根拠を添える。
8. **数値には出所を添える。** 出所が分からない数値は書かない。未測定なら「要測定」と書く。
9. **表の下に注を 1 つ置く。** 読み方と条件を説明する。
10. **ページ間の参照はリンクにする。** 「§2 を参照」だけでは読めないため、リンクと節名を添える。
11. **文体は「です・ます」調で統一する。** 手順の指示は「〜してください」と書く。
12. **見出しは名詞句で短くする。** 節の 1 行目で、その節が何を述べるかを書く。

---

## 3. 数値の表記

| 対象 | 規則 | 例 |
|---|---|---|
| 小さい値（`Shift`、相対距離） | 指数表記で有効数字 3 桁 | `5.21e-05`（`0.0000521` とは書きません） |
| パーセント | 小数 1 桁 | `8.3%` |
| 合否の境界 | 指数表記 | `> 2.1e-04` |
| ファイルパス、コマンド、識別子 | バッククォートで囲みます | `` `docs/isp/criteria.md` `` |
| 細胞数、遺伝子数 | 3 桁ごとに区切ります | `4,126` |

**実測には条件を添えます。** モデル、組織、細胞数、遺伝子数、数値精度を書きます。
条件のない数値は引用しません。出所はファイル名か実行名で示します
（例: `docs/quantization/REPORT-316M-ISP-jp.md`、実行名 `isp-316m-bf16-full`）。

### 3-1. 言語

| 対象 | 規則 |
|---|---|
| 本文（説明文） | 日本語です。 |
| コード識別子、ファイル名、ディレクトリ名 | 英語のまま書きます（`Shift_to_goal_end`、`max_ncells`）。日本語に置き換えるとコードと本文が対応しなくなります。 |
| 指標名、関数名、パラメータ名 | 英語のまま書きます。初出で日本語の説明を一度だけ添えます。 |
| コミットメッセージ | 英語で書きます（1 行目に種別と要約、本文に理由）。 |

### 3-2. `WT` と `Control` は同じ状態です

データの `Diagnosis` 列の値は `AD` と `Control` ですが、ISP の `goal_state` は `WT` です。
初出で「対照（データ上は `Control`、ISP 上は `WT`）」と一度だけ明記し、以降は文脈で統一します。

---

## 4. 根拠と判定

### 4-1. 根拠の種類

**根拠の種類を必ず添えます。** A は自分たちの実測、B は他組織や他データからの借用、C は未確立です。

- **合否の線に使えるのは A だけです。** C の基準で判定した場合は「暫定」と明記します。
- **「基準」と「目安」を区別します。** 目安は合否に使わず、解釈を控える判断に使います。

### 4-2. 判定の語彙（4 つだけ）

| 判定 | いつ使うか |
|---|---|
| **合格** | 基準を満たした |
| **不合格** | 基準を満たさなかった |
| **未検証** | その基準を確かめる実行をしていない |
| **判定不能** | 測ったが、データでは決められない |

個々の数値が未測定のときは「要測定」と書きます（§2.8）。

> **どの基準をどの解析に適用するかは、解析を始める前に合意します。**
> 事後に基準を当てはめて判定を出すことはしません。

---

## 5. 文書の分担

| 文書 | 書くこと | 書かないこと |
|---|---|---|
| [README](README.md) | 索引と全体の状況 | 個別の数値の詳細 |
| [評価基準](criteria.md) | 基準と目安、根拠の種類、判定の手順 | 実行手順の詳細 |
| [用語と手法の定義](glossary.md) | 用語の意味と使い方 | 表記の可否（このページに書く） |
| **記述ルール（このページ）** | 用語の統一表、表現、数値、根拠と判定 | 個別の解析結果 |
| [遵守項目](checklist.md) | 実行時の行動規範 | 基準の数値そのもの |
| [レポートの標準構成](report_template.md) | レポートの節立て | 文体のルール（このページに書く） |
| [結果](results.md)、[ad_bm](ad_bm.md)、[ad_liver](ad_liver.md)、[pd](pd.md)、[pd_atlas](pd_atlas.md) | 各実行の結果と解釈 | 基準の再定義 |

---

## 6. 骨格

各ページはこの順で書きます。

1. **目的**: そのページは何のためにあるかを 1 段落で書きます。
2. **前提**: 対象データ、モデル、条件（組織、時点、細胞数、数値精度）を書きます。
3. **本文**: 結果、解釈、限界を書きます。
4. **出所**: 数値と引用の出典を書きます。

---

## 7. レポート（DOCX、PDF）

- 節立ては [レポートの標準構成](report_template.md) に従います。
- 冒頭の要約は 5 行以内にまとめ、**目的と結論を先に**書きます。プロジェクトの経緯は書きません。
- 用語表を必ず付けます。
- 日本語版と英語版を作ります。ファイル名は英語を基本とし、日本語版には「-jp」を付けます。
- 数値、図、表はテンプレートのスクリプトで再生成できる状態にします。

---

## 8. 用語や基準を変えるときの手順

1. まずこのページの [§1 用語](#1-用語これだけを使う) を直します（表が唯一の正です）。
2. 次に [評価基準](criteria.md) の識別子（E1〜E6）と、**各ページからの参照**を直します。
3. 競合や重複が起きた場合は [§9](#9-競合重複の記録) に追記します。
4. 最後にレポートを再生成し、日本語版と英語版の両方を差し替えます。

---

## 9. 競合・重複の記録

> **この節は、以前の記述と内容が重複しています。**
> 経緯を知らないと「なぜこの語に決まったのか」が分からず、同じ議論を繰り返すため、
> **裁定の記録として残しています。** 本文の規則と食い違ったら、本文（§1〜§8）が正です。

複数のエージェントが並行して書いたため、次の衝突が起きました。**すべて裁定済みです。**

| # | 衝突していた内容 | 裁定 | 状態 |
|---|---|---|---|
| 1 | **ルールブックが 2 つ**（`writing_standard.md` と `style-guide.md` が同時に作られた） | `style-guide.md` を唯一の基準とし、`writing_standard.md` は削除 | 完了 |
| 2 | **判定の語彙**（`report_template.md` は「合格・不合格・保留」、`checklist.md` は「判定不能」、ズレ点検表は「未達」） | §4-2 の 4 語に統一。「保留」「未達」は使わない | 完了 |
| 3 | **基準の識別子**（`C1〜C6` と `E1〜E6` が併存。改名が参照側に未反映） | `E1〜E6`。`glossary.md` と `report_template.md` の旧称参照を修正 | 完了 |
| 4 | **基準の性質**（実測由来の `2.1e-04` と、根拠のない目安〔IQR の 2 倍、500 細胞、15% 分離〕が同じ「基準」として合否に使われていた） | [評価基準](criteria.md) で根拠を A・B・C に分離。**目安は合否に使わない** | 完了 |
| 5 | **`pd_atlas` への基準適用**（事後に当てはめて「不合格」と書いた） | 適用は**事前合意が必要**。当該実行では未合意のため判定を出さない | 完了 |
| 6 | **相対距離の式**（`glossary.md` の例〔cos 0.9966 → 8.27%〕から逆算した式で計算した） | 実装で確認するまで**推定値として扱う**。判定には使わない | **未了**（実装確認待ち） |
| 7 | **用語の併存**（発現率/検出率、遺伝子パネル/遺伝子リスト、fine-tune/ファインチューン/微調整、プール/細胞プール） | §1 の統一表で裁定 | 完了 |
| 8 | **数値表記の混在**（十進 `+0.002132` と指数 `2.13e-03`） | 指数表記・有効数字 3 桁に統一（§3） | 完了 |
| 9 | **同じ節を 2 人が別々に書き、片方が上書きされた**（`pd_atlas.md` の領域間比較の節など） | 内容を §9 の記録として残し、本文は 1 人分に統合 | 完了 |

---

## 10. 適用状況

| 項目 | 状態 | 備考 |
|---|---|---|
| 微調整（`fine-tune`、`Fine-tuned`、`ファインチューン`） | **完了** | 日本語ページを全て置き換えました。英語版（`REPORT-316M-ISP.md`、`EXPERIMENT.md`）は英語の文書のため `fine-tune` のままとしています |
| 判定の語彙（`保留`、`未達`） | **完了** | `report_template.md` から消え、`未検証` と `判定不能` に統一しました |
| 基準の識別子（`E1〜E6`） | **完了** | 旧称 `C5`・`C1〜C6` の参照を修正しました |
| 数値の表記（指数表記・有効数字 3 桁） | **完了** | 結果表の十進表記を置き換えました |
| 相対距離の算出式の実装確認 | **未了** | 確認できるまで `状態分離` を判定に使えません（§9 の #6） |