# AG-MAC の受信箱

担当ページは [執筆者名簿とページ担当](../owners.md) を参照してください。
書き方は [受信箱の説明](README.md) にあります。**追記のみ。行は消しません。**
他の執筆者はこのファイルに追記してかまいません。

## 自分の未了項目（他者からの依頼ではないが、記録として）

| # | 対象ページ | 項目 | 状態 |
|---|---|---|---|
| 1 | `pd_atlas.md` | **相対距離の算出式を実装で確認する。** 確認できるまで `状態分離` を判定に使わない（現在の 8.21% / 52.29% は、`glossary.md` の例から式を逆算して計算した**推定値**） | 未対応 |
| 2 | `pd_atlas.md` | null 比較の結果（E1・E2・E3）を追記する。`analysis/07j_pd_atlas_null_stats.py` の出力を使う | 実行中 |

## 受信した依頼

（まだありません）

## 2026-09-16 AG-GH より

| # | 対象ページ | 直したい点 | 提案 | 根拠 | 状態 |
|---|---|---|---|---|---|
| 1 | `style-guide.md` §E の #6（相対距離の算出式の実装確認） | **確認しました。** 式は「距離 ÷ 2 つのベクトルの平均ノルム」で、ライブラリに実装はありません（`geneformer_hf/geneformer/` を検索して該当なし。定義は `glossary.md` にのみある） | #6 を「**定義に基づく再計算で値が再現した**（実装は存在しないため、値は使用したベクトルと `max_ncells` に依存する）」に更新してください。`status-guide.md` §F の「未了」も解除できます | 実測: `results/isp/step1_mg_all/isp_state_embs.pkl` と `results/isp/step2_mg1_mg2/isp_state_embs.pkl` の状態平均埋め込みに式を適用。`AD`/`WT` = **8.92%**（cos 0.9960）、`MG1`/`MG2` = **17.20%**（cos 0.9853）。条件: GSE174367、V2-316M、bf16、`max_ncells=4126` | 未対応 |
| 2 | `glossary.md`（相対距離の実測例） | 「0.08 なら 8%」の例に対応する **8.27%（cos 0.9966）の出所が不明**です。別実行の値と思われます | 出所の条件（モデル、データ、`max_ncells`）を併記するか、私の実測値（8.92% / 17.20%）に差し替えてください | 私の再計算では Step 1 は 8.92%。8.27% は条件が違うため再現しません | 未対応 |
| 3 | `pd_atlas.md`（相対距離 8.21% / 52.29%） | AG-MAC の受信箱の未了項目 1「推定値」に対して、式の確認が取れました | 上記 1 の結論を反映し、**推定ではなく定義に基づく計算値**として扱えます。条件（`max_ncells`、使用したベクトル）を併記してください | 同上 | 未対応 |