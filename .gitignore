# ---- Python / env ----
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- local workspace / generated artifacts (NOT for the public repo) ---
# .venv holds torch + geneformer (~1.5 GB) — recreate via `uv venv` + README
/.omc/
/.omo/
/analysis_ws/
/input/
/rds2h5ad/rds_data/
/rds2h5ad/ws/
/rds2h5ad/renv/
/rds2h5ad/pyenv/
/rds2h5ad/env.sh
/rds2h5ad/manifest.json
/rds2h5ad/.Rprofile
/rds2h5ad/renv.lock

# --- genesformer repository (separate clone) ---
# geneformer_hf/ is a git-lfs clone of ctheodoris/Geneformer (7 GB incl. weights).
# Not committed. Fetch with ./download.sh. Our device patches live in patches/.
/geneformer_hf/

# --- quantized / pre-converted model artifacts (regenerable, ~1.5 GB) ---
# Built by analysis/11_prequant_vs_loadtime.py. See docs/quantization/.
/quantized/
analysis/.omc/
analysis/.omo/
analysis/.DS_Store

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/
*.log
*.log.bak*
/nohup.out

# --- in-silico-perturbation computed results (NOT uploaded; regenerable) ---
# Per-gene Shift_to_goal_end CSVs + figures + generated HTML reports are
# computed/generated artifacts. Keep the build/render scripts + templates +
# markdown reports tracked; the heavy numeric outputs / rendered HTML are not.
/result/report/

# --- ref data (MGI homology; auto-downloaded by 03 script, symlink re-created) ---
ref
analysis/ref
# --- in silico perturbation COMPUTED RESULTS (not for the public repo) ---
# Raw perturbation datasets / state-embeddings / per-gene raw pickles
/input/*/results/isp/
**/results/isp/
/input/*/tokenized/*_early.dataset/
# Report computed artifacts (figures + result tables), not source
**/results/report/figures/
**/results/report/experiments_mapping.csv
**/results/report/*.html
/result/report_ad_liver/assets/*.png
/result/report_ad_liver/assets/isp_gene_ranking.csv
/result/report_ad_liver/isp_gene_ranking.csv
/result/report_ad_liver/*_early_isp_summary.csv
/result/report_ad_liver/*_early_isp_gene_ranking.csv
# PD per-tissue report dirs: computed/generated artifacts (not uploaded)
/result/report/assets/
/result/report_bm/assets/
/result/report_blood/assets/
/result/report_bm/*_early_isp_summary*.csv
/result/report_bm/*_early_isp_gene_ranking*.csv
/result/report_blood/*_early_isp_summary*.csv
/result/report_blood/*_early_isp_gene_ranking*.csv
/result/report_bm/report.html
/result/report_blood/report.html
# AD per-tissue report dirs: computed/generated artifacts (not uploaded)
/result/report_AD_blood/
/result/report_AD_smallint/
/result/report_AD_spleen/
/result/report_pd_spleen/
# ISP result aggregation dir (computed/generated artifacts)
/isp_result/

# --- ISP model comparison / literature check (local only, not for the repo) ---
# Produced by analysis/16-21_*.py on request. The reports (PDF/DOCX), figures,
# literature records and derived data stay on this machine; do not commit them.
/docs/quantization/isp-model-comparison/
/docs/quantization/expression/
# analysis/16-21_*.py are committed as TEMPLATES (see the banner in each file).
# They embed an example verdict table / report prose / search queries; the
# outputs they produce stay local (see the rules above).
